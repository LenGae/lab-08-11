<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        xmlns:vm="clr-namespace:WpfApp8.ViewModels"
        xmlns:conv="clr-namespace:WpfApp8.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <conv:CutOffConveter x:Key="cutOffConveter" Min="4"/>

        <Style TargetType="Image" x:Key="ImgAir">
            <Setter Property="Height" Value="140"/>
        </Style>
    </Window.Resources>

    <d:Window.DataContext>
        <vm:ViewModel/>
    </d:Window.DataContext>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

        <ItemsControl ItemsSource="{Binding Collection}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Padding="5" Margin="5" BorderThickness="1" BorderBrush="Black">
                        <DockPanel>
                            <StackPanel>
                                <TextBlock>
                                    <Run Text="{Binding AircraftId}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding Model}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding Type}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding RegistrationNumber}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding SeatingCapacity}" />
                                    <Run Text="|"/>
                                    <Run Text="{Binding Range}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding Speed}"/>
                                    <Run Text="|"/>
                                    <Run Text="{Binding CreatedAt}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Description, StringFormat='Описание: {0}'}"/>
                                <TextBlock TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding BasePricePerHour, StringFormat='Цена: {0}'}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Information}" Value="">
                                                    <Setter Property="Text" Value="Нет цены"/>
                                                    <Setter Property="Opacity" Value="0.4"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock>
                                    <Image Source="{Binding ImageUrl}" Style="{StaticResource ImgAir}"> </Image>
                                </TextBlock>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Window>

<Window x:Class="WpfApp8.AddUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Title="Добавить пользователя" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Полное имя:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="FullNameTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Телефон:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="PhoneTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Пароль:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Роль:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="4" Grid.Column="1" Margin="5" SelectedIndex="0">
            <ComboBoxItem Content="Пользователь" Tag="1"/>
            <ComboBoxItem Content="Менеджер Авиапарка" Tag="2"/>
            <ComboBoxItem Content="Администратор Системы" Tag="3"/>
        </ComboBox>

        <TextBlock Text="Активен:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <CheckBox x:Name="IsActiveCheckBox" Grid.Row="5" Grid.Column="1" Margin="5" IsChecked="True"/>

        <Button Content="Добавить" Grid.Row="6" Grid.ColumnSpan="2" Margin="5" Height="35"
                HorizontalAlignment="Center" Width="120"
                Command="{Binding AddUserCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />


    </Grid>
</Window>
